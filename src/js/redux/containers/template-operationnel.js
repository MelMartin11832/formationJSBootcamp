import { connect } from "react-redux";
import { TemplateOperationnel } from "js/components/tableau-de-bord";
// import { chargerTemplatesOperations } from "js/redux/actions";

const tOpToProps = state => {
  return {};
};

const tOpDispatch = dispatch => ({});

export default connect(tOpToProps, tOpDispatch)(TemplateOperationnel);
